import { useEffect, useState } from "react";
import useApiCall from "./useApiCall";
import { products } from "../api/data";

export default function useDataHomePage() {
    const [data, setData] = useState({});
    useEffect(() => {
        
    },[])
}